# === Segreti — MAI nel repo ===
config.php

# === Stato volatile del server ===
data/throttle.json

# === File generati dal deploy (sincronizzati da hub / stato osservato) ===
app/assets/catalogo.js
app/assets/stato-osservato.js

# === Sistema ===
.DS_Store
